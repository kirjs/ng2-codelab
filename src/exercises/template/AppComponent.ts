import {Component} from '@angular/core';

const FAKE_VIDEOS = {
  videos: [
    {
      name: "cat video"
    }
  ]
};

@Component({
  selector: 'my-app',
  templateUrl: 'app.html'
})
export class AppComponent {
}
